[PROMPT]
default=(?<=\s)\S+#
pageoff=terminal length 0

[CONFIG]
show=show config
showrun=show running-config view full
filter=(?<=secret \d )\S+(?=\s+)
filter2=(?<=key )\S+(?=\s+)
filter3=(?<=community )\S+(?=\s+)

[VERSION]
cmd=show version
filter=\d+ weeks, \d+ days, \d+ hours, \d+ minute

[HW]
cmd=show flash
cmd2=show boot
cmd3=show switch

[VLAN]
cmd=show vlan
